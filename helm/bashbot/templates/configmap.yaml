apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.botname }}-configmap
  namespace: {{ .Values.namespace }}
data:
  {{- $files := .Files }}
  {{- range tuple "config.json" ".tool-versions" }}
  {{ . }}: |-
{{ $files.Get . | indent 3 }}
  {{- end }}